import { Typography } from "@mui/material";
import React from "react";

const Home: React.FC = () => {
    return (
        <div className="Home">
            <Typography variant="h1">Home</Typography>
        </div>
    );
};

export default Home;
